using UnityEngine;

namespace Failsafe.DemoScripts
{
    public class ObjectSpin : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
